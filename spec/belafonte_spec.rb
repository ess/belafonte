require 'spec_helper'

describe Belafonte do
  it 'has a version number' do
    expect(Belafonte::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
